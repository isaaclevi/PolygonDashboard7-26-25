{
  "name": "stockpolygondashboard7.22.25",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start": "node dist/server.js",
    "db:init": "ts-node src/utils/db-init.ts",
    "loadbalancer": "ts-node-dev --respawn --transpile-only src/loadbalancer-server.ts",
    "loadbalancer:start": "node dist/loadbalancer-server.js",
    "backend:dev": "ts-node-dev --respawn --transpile-only src/server.ts --port 3001",
    "backend:dev:2": "ts-node-dev --respawn --transpile-only src/server.ts --port 3002",
    "backend:dev:3": "ts-node-dev --respawn --transpile-only src/server.ts --port 3003"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",

    "pg": "^8.16.3",
    "winston": "^3.17.0",
    "ws": "^8.18.3",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "@types/pg": "^8.15.4",
    "@types/ws": "^8.18.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
