import { VolumeFormatPipe } from './volume-format-pipe';

describe('VolumeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VolumeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
